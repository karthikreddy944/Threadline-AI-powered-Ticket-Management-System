const express=require("express"); const {protect}=require("../middleware/authMiddleware"); const {getAuthUrl,callback,status,repos,selectRepository,disconnect}=require("../controllers/githubController"); const router=express.Router(); router.get("/auth-url",protect,getAuthUrl); router.get("/callback",callback); router.get("/status",protect,status); router.get("/repos",protect,repos); router.post("/repository",protect,selectRepository); router.delete("/connection",protect,disconnect); module.exports=router;
